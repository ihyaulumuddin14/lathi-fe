'use client'

import { useSlides } from "@/hooks/useSlides"
import { useEffect } from "react"

export default function PlaySessionPage() {
   // const { slides, error, isLoading } = useSlides()

   // useEffect(() => {

   // }, [])

  return (
   <section>Game UI</section>
  )
}
